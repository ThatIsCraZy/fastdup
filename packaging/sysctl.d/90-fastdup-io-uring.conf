# fastdup's durable DATA publisher requires io_uring. It deliberately has no
# synchronous fallback because publication ordering is part of the durability
# contract (ADR 0058).
kernel.io_uring_disabled = 0
