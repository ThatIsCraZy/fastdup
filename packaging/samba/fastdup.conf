# Include this file once from the global section of smb.conf:
#   include = /etc/samba/fastdup-shares.conf
#
# The Control Plane atomically replaces fastdup-shares.conf only after a
# successful `testparm -s` validation and then reloads Samba with smbcontrol.

