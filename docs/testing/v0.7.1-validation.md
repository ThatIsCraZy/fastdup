# v0.7.1 validation — 9 September 2026

Package `fastdup-0.7.1-1.el10.x86_64.rpm`, built from the release changes on a
worktree excluding unrelated checkpoint work in progress.

- 196 Rust library/binary tests (Store, appliance, control) passed after version bump.
- Two metadata-clone integration tests passed, including the exact reported
  offsets, zero DATA I/O at clone/checkpoint and metadata fault injection.
- 39 WebUI tests passed; TypeScript/Vite production assets rebuilt and embedded.
- Portable Samba contract suite passed; module built against Samba 4.23.5.
- Binary/source RPM creation and digest checks passed; SHA-256 manifest produced.
- Earlier unchanged-code validation includes 17 manifest/prefix/differential
  read tests and production Clippy with warnings denied.

Before installation, the original failure was also reproduced over SMB 3.1.1
on the test VM using a read-only source handle and a uniquely named temporary
target: `STATUS_INVALID_PARAMETER`, cluster size 65536, source offset 1617920,
target offset 1609728, length 8192. The test removed only its temporary target.

Logs and reproduction scripts: `.artifacts/v0.7.1/` in the build workspace.

## Test VM activation

Upgraded 10.1.1.161 from `0.6.4-14` to `0.7.1-1` with verified package hashes,
no active SMB shares, a successful managed unmount, and orderly service restarts.
The new agent automatically mounted the existing repository and regenerated the
managed share with `clone alignment = 4096`. Repository, agent, control and Samba
are active; runtime telemetry includes the new admission counters and usage data.
RPM verification reports only the intentionally customized managed configuration
files, with no executable or module differences.

The same SMB 3.1.1 Duplicate Extents request now succeeds: source 1617920,
target 1609728, length 8192, cluster 4096, effective Integrity identifier 1.
One measured clone call took 0.004625 seconds. The bytes and both neighboring
bytes match, including after flush and reopen. The source's stored `0200` xattr
remains unchanged. The unique temporary target was removed. This one-call timing
is functional smoke evidence, not a backup throughput benchmark. A full Veeam
synthetic-full retry is still required for application-level qualification.
